# 下周的葬礼

一款以浏览器调查为核心的悬疑解谜游戏原型。

> **剧透提示：这是公开开发仓库，源码和 docs 含剧情、谜题答案及结局草稿。**
> 只想游玩的玩家请使用游戏网址，不要阅读源码和设定。

- 源码仓库：https://github.com/gongmenglin126/next-week-funeral
- GitHub 公开试玩：https://gongmenglin126.github.io/next-week-funeral/
- 现有试玩：https://next-week-funeral.gonglin556.chatgpt.site （访问权限由站点分享设置决定）
- 正式人物名：主角 **林知还**，朋友 **周惜**；订单页面目前保留“林** / 周**”脱敏显示。
- GitHub Pages 已启用，发布目录为 `gh-pages` 分支根目录；源码仍在 `main`。2026-08-31 首次部署已成功。
- [最新决定与交接状态](docs/04-current-decisions-and-handoff.md) 优先于初版蓝图中的冲突内容。

玩家与大学好友已经开始了一场约定多年的旅行。两人因不同的冲突处理方式产生争执：她把玩家暂时的沉默理解成厌恶与抛弃，玩家却在冷静后回来，明确告诉她“我是在生气，不是不要你”。第二天，她借口外出买药，却在前往终止秘密安排的途中意外去世。她的电脑留在民宿，玩家从处理剩余订单开始，逐渐发现一个以生命关怀为公开外壳、由顾惟真的个人神话支撑的秘密教团。周惜曾因死亡恐惧把林知还带进第七期活动，后来决定退出，却没能活着完成线上取消；真正的好结局必须由独立证据证明她最后的反悔。

## 当前版本

当前可玩的第一章已包含：

- 独立的全屏故事序章与电脑入口；
- 可操作的朋友电脑桌面与浏览器入口；
- 自适应浏览器窗口；
- 独立记事本，保存可手动勾选的五项行程清单（不是线索簿）；
- 搜索“泊岸旅行”进入统一旅行平台，五笔订单都在“我的订单”；
- 五种取消交互：直接退款、电子票尾号校验、退款规则确认、票面取消预约、逐人退回程票；
- 订单分类、状态筛选、检索、退款回执和帮助页面；
- 五笔真实取消后，在当前画面立即弹出一次安时接送通知；可从通知中心找回，不转场、不跳时；
- 通知进入独立的双人夜间接送订单，支持查看机构信息与尝试取消；
- 可访问的本地论坛与帖子；
- 浏览历史与下载记录；
- 下载和文件夹不再重复提供共同旅行计划，行程清单只保留在记事本；灯塔电子票仍可打开；
- 相册保留晚餐与海岸两张不同照片；回收站有一张走廊裁剪副本，可预览、缩放并恢复到相册；不把恢复副本冒充恢复原图；
- 可输入的模拟搜索；
- 雾搜页面有独立搜索框与搜索按钮，支持回车提交；上方地址栏仍可输入网址或搜索；
- 关键词搜索结果与历史快照提示；
- 响应式布局；
- 核心设定、章节蓝图和谜题原则文档。

当前版本完整覆盖“清单 → 搜索旅行平台 → 五笔取消 → 未列出接送”的第一章。记事本勾选不会提前触发剧情，回程只退一张票也不会算完成。跨应用和标签切换保留状态，真实浏览器刷新后从头开始。裁剪副本的恢复状态也只在当前游玩会话保留。

后续主线仍未接成完整可通关流程，但调查材料已经分层接入：隐藏第七期、归潮见证社区、米粒寻猫与程叙白讣告、顾惟真从信佛到失信的海岬经历、无面小像、栖潮书院的真实援助与选择性神化，以及 R-06-4 修订单—S-17 内部规则—项目说明会纪要。周惜的报名、最后一次退出和终章手机仍待实现。所有账号、预订和退款均为游戏内模拟，不会访问真实账号或资金。

## 本地运行

需要 Node.js 22.13.0 或更新版本。保留 package-lock.json；不要直接升级依赖后再判断旧版本能否运行。

```bash
npm ci
npm run dev
```

生产构建：

```bash
npm run build
```

现有 Sites 构建脚本使用 Bash 和 GNU timeout，推荐 Linux / WSL；其他环境可运行 `npx vinext build`。该构建输出是 Vinext/Worker 应用，不要直接上传到 GitHub Pages。

### GitHub Pages 静态版

```bash
npm ci
npm run build:pages
```

独立的 `vite.pages.config.ts` 复用同一套游戏组件，输出 `pages-dist/`，不需要 Worker、数据库或服务器。构建会检查 HTML、脚本、样式与三张游戏图片在 `/next-week-funeral/` 下的引用，并生成 `.nojekyll`。原来的 Sites 构建保持不变。

将 `pages-dist/` 的内容提交到 `gh-pages` 根目录。首次在 [Pages 设置](https://github.com/gongmenglin126/next-week-funeral/settings/pages) 选择 **Deploy from a branch → gh-pages → / (root) → Save**。待 GitHub 的 Pages 部署成功后，游戏地址为 `https://gongmenglin126.github.io/next-week-funeral/`。准备好分支不等于站点已启用。

后续更新需重新构建并更新 `gh-pages`；仅修改 `main` 不会自动更新试玩。本项目暂未配置自动发布工作流。

这里只发布运行文件，不把 docs、源组件文件或 source map 放进站点目录。不过公开仓库仍能查看源码，浏览器也能检查收到的 JavaScript；静态发布不提供隐藏谜底的安全保证。

只运行第一章状态机测试（不重复构建）：

```bash
node --experimental-strip-types --test tests/chapter-one.test.mjs
```

覆盖全部120种取消顺序、错误校验、分人退票、重复点击、通知关闭后的可找回状态以及手动记事本标记独立性。

### 检查记录（2026-08-31）

- `npx vinext build`：通过。
- 首章状态机：7/7通过，包含全部120种取消顺序。
- 首次上传的完整测试集：12/14通过。两项既有模板检查失败：开发预览 meta 标记缺失、目录模板的滚动样式工具类缺失；保留原测试，没有删除或修改断言来制造全绿。
- 交互清理后的首章与交互契约测试：11/11通过，覆盖按钮事件连接、素材存在性、裁剪副本恢复前后的列表与预览控件。这些是代码与组件渲染检查，不等同于逐项浏览器端点击验收。
- 当前仅为第一章可玩原型，不能将构建通过当作完整剧情已经实现。

## 本次内容与交互清理

- 删除空的“给你.txt”、重复晚餐图片及不相关的盐场照片附件。
- 删除无法打开的地图、个人主页历史项，以及无行为的论坛统计、系统菜单、刷新和新建标签入口。
- 文件侧栏现在可真正切换文件、下载、相册和回收站；记事本从桌面图标或底部快捷入口打开。
- 历史记录可直接打开对应论坛帖子；前进后退恢复文件预览，标签切换保留已访问页面入口和搜索词。
- 照片支持缩放、上一张/下一张、文件信息；恢复裁剪副本后回收站与相册同步更新。
- 订单缩略图可进入订单详情；没有对应素材的订单使用类别图标，不拿晚餐照冒充盐场。

## 项目文档

- [核心设定](docs/00-core-setting.md)
- [浏览器主线](docs/01-browser-game-flow.md)
- [谜题原则](docs/02-puzzle-principles.md)
- [终章备忘录草稿](docs/03-ending-note-draft.md)
- [最新决定与交接状态](docs/04-current-decisions-and-handoff.md)

## 当前硬约束

- 主要玩法发生在浏览器，不扩展成大而全的桌面模拟器。
- 不设置自动整理证据的“线索簿”；信息保留在网页、标签页、历史和下载中。
- 搜索只用于验证玩家已发现的关键词，不能成为自动发放线索的唯一入口。
- 宗教分层出现，不能一点进去就看到完整教义。
- “一滴泪的重量，取决于落在谁身上”只属于终章手机备忘录，不出现在宗教材料中。
- 朋友自卑、敏感，但“自卑 / 同性恋”都不是她作恶的原因。
- 她曾经真实地选择过玩家，最终也真实地决定取消。
- 好结局必须由玩家推理证明，不能依靠一句遗书洗白。
